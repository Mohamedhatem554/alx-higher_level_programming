#!/usr/bin/python3
"""2-is_same_class"""


def is_same_class(obj, a_class):
    """define func

    Args:
        obj:object
        a_class: first class

    Returns:
        type
    """
    return type(obj) == a_class
